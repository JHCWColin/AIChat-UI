# AIUI 更新日志

## V5.3.0 · 2026-08-02

- 语音聊天模式微调了黑白字幕字号与行间距，长段文本更紧凑。
- AI 回应字幕改为按句自动跟随滚动，当前播报句会尽量保持在视图中部。
- 自动跟随滚动不会锁死用户操作；用户手动滚动后，5 秒无操作会回归自动跟随。
- 语音聊天中 AI 生成文字与用户识别文字都加入了淡入动画。
- 版本升级至 V5.3.0，并同步更新 Electron 构建产物命名。

## V5.2.0 · 2026-08-02

- 图像生成与图像编辑支持在提示词中使用宽高比关键词，例如 `16:9`、`4:3`、`1:1`。
- 自动识别宽高比并转换为图像请求的 `size` 参数；未填写比例时继续使用 `1024x1024`。
- 比例关键词不会作为额外文本发送给图像模型，原始提示词仍会完整保留在聊天记录中。
- 图像生成重试会保留原始提示词并重新计算比例参数。
- 版本升级至 V5.2.0，并重新生成 Windows Portable 与 Setup 安装包。

## V5.1.0 · 2026-08-02

- 语音聊天窗口重构为纯黑 / 纯白双区域界面，跟随主窗口深浅主题。
- 语音聊天继续使用 Silero VAD、讯飞流式听写和 Fish Audio HTTP TTS。
- 语音聊天支持把完整人类 / AI 轮次回传并拼接到当前文字对话。
- API 配置新增可编辑模型列表，支持增加、删除、排序并保存到 LocalStorage。
- 新增默认模型：`claude-opus-5`、`gemini-3.6-flash`、`gemini-3.5-flash-lite`、`gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`。
- 新增上下文压缩功能：支持自动摘要、手动 `/compact`、加号菜单压缩、独立摘要模型和开发者日志记录。
- 原始聊天消息保留不变，摘要仅用于减少发送给模型的上下文长度。

## V5.0.0 · 2026-08-02

- 新增 Electron 语音聊天入口和独立 `audiochat.html` 页面。
- 语音聊天支持已有文字上下文通过 IPC 传入，以及结束通话后将语音轮次回传当前对话。
- 新增 Fish Audio 预制语音 A/B 组生成与主进程文件存储。
- 新增讯飞流式语音听写和 Fish Audio HTTP TTS。
- 语音窗口单独放宽网页安全策略，主窗口保持安全隔离。
- 修复用户消息与对话标题的纯文本处理、HTML 转义、Markdown/KaTeX 禁用问题。
- 修复偏好设置右侧内容区域未填满的问题。
- 增加 Windows Portable 与 Setup 构建配置。
