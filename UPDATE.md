# AIUI 更新日志

<!--
格式要求：以后新增版本记录时必须严格使用以下结构，版本标题和日期之间使用“ · ”，正文只使用“- ”开头的单行列表项。

## V版本号 渠道 · YYYY-MM-DD

- 更新内容一。
- 更新内容二。

不要在版本标题与下一版本标题之间加入其他级别的标题，也不要省略版本正文；关于页会按此格式自动解析展示。
-->

## V7.0.0 Canary 4 · 2026-08-14

- Agent 协议解析新增 `<json>`、`</json>` 及其 HTML 转义形式的清理，折叠工具调用附近的多余空行。
- Agent 可见消息在进入 Markdown、代码高亮和 KaTeX 前统一剥离已识别的 Tool Calling JSON，历史会话重绘时也不会重复显示工具对象。
- Agent 模型流式响应按 SSE `id` 或 `sequence_number` 去重，修复后续任务中重复事件再次拼接整段 JSON 的问题。
- `finish_task` 提示嵌入最终 AI 回复内部，固定排列在回复正文之后、模型名称和复制按钮之前。
- 增加协议与 Electron 界面回归验证，覆盖 JSON 包装清理、工具 JSON 不可见、最终提示 DOM 顺序和中文 Shell 输出。
- 版本升级到 `V7.0.0 Canary 4`，构建 Windows Portable / Setup 产物。

## V7.0.0 Canary 3 · 2026-08-14

- Agent 模型回复改为按原始顺序解析正文段与工具段，支持“正文 → 工具 → 正文 → 工具”的交错展示；已识别的 Tool Calling JSON 不再进入可见正文或重复显示。
- Shell 运行状态改用毫秒显示，并强制 PowerShell 标准输入输出使用 UTF-8，修复中文命令结果出现替换字符的问题。
- Agent 工具 Lucide 图标随聊天字体设置缩放，图标尺寸与一个汉字接近。
- 非白名单命令授权移动到聊天正文，使用与工具提示相同的宽度和排版；移除命令前缀输入框，“始终允许”直接保存自动推导的前缀。
- 修复非 Canvas 普通会话错误显示无效 Canvas 按钮；Agent 输入框右上角改为直角，与 Command Mode 选择框无缝衔接。
- 补充自动压缩回归验证：Agent 的 10 轮按十个已完成用户任务计数，内部模型循环和工具调用不计入轮次。
- 版本升级到 `V7.0.0 Canary 3`，构建 Windows Portable / Setup 产物。

## V7.0.0 Canary 2 · 2026-08-14

- Agent 工具提示字号跟随偏好设置中的聊天字体大小，并缩小连续工具调用之间的垂直间距。
- `write_file` 与 `edit_file` 在工具提示下显示带颜色的增删差异，`run_shell` 显示标准输出与错误输出；详情可点击提示行展开或折叠。
- 工具详情默认最多显示 10 行，超出时保留首尾并标注中间省略行数；Agent 设置可切换为 20、50 行或不省略。
- Command Mode 与模型选择器并排附在输入框上方，移除额外文字标签并统一选择框外观；命令授权区域与普通工具提示使用一致宽度。
- 工作区路径移动到标题同行并向右对齐；Agent 设置新增 5 至 3600 秒的命令执行超时配置，默认 120 秒。
- 版本升级到 `V7.0.0 Canary 2`，构建 Windows Portable / Setup 产物。

## V7.0.0 Canary 1 · 2026-08-14

- 新增 Agent 模式入口；会话绑定工作区后不可退出、不可更换，并在标题栏显示工作区路径。
- 新增标准正文 JSON Tool Calling 的连续 Agent Loop，支持单次模型回复最多 30 个工具调用，并以 `finish_task` 结束任务。
- 新增 `read_file_range`、`write_file`、`edit_file`、`run_shell` 和 `finish_task`，文件工具严格限制在工作区内。
- 新增 Safe Commands Only 与 YOLO Mode；未知 Shell 命令支持拒绝、允许和始终允许，YOLO 切换显示全屏风险警告。
- 新增 Agent 偏好设置，可管理用户自己的始终允许命令前缀；内置白名单文件只读展示。
- 工具结果完整写入 Agent 上下文并通过 IndexedDB 保存大结果，不参与自动摘要删除；Agent 运行期间锁定会话切换和删除。
- 版本升级到 `V7.0.0 Canary 1`，构建 Windows Portable / Setup 产物。

## V6.3.0 Release · 2026-08-13

- 自动更新检查确认当前版本已是 GitHub Release 最新版本时，在“关于信息”的版本号后以绿色文字显示“当前已是最新版本”。
- 版本升级到 `V6.3.0 Release`，由 GitHub Actions 重新生成并发布 Windows Portable / Setup 安装包及自动更新元数据。

## V6.2.0 Release · 2026-08-13

- Windows Setup 安装版接入 `electron-updater`，每次启动时可从 GitHub Release 检查新版本；Portable 版本和开发环境不会触发更新检查。
- 偏好设置“其他”新增“自动探测更新”，默认开启，用户可手动关闭；重新开启时会在本次进程尚未检查过的情况下立即检查。
- 发现新版本后使用 Electron 系统级原生对话框询问是否下载，下载完成后可立即重启安装或在退出应用时安装。
- 更新下载复用 Electron Builder 生成的 `latest.yml` 和 NSIS `.blockmap`，由 `electron-updater` 自动执行差分下载。
- “关于信息”的版本号后会在发现更新时显示可用的新版本号；当前已是最新版本时不显示额外提示。
- GitHub 发布源固定为 `JHCWColin/AIChat-UI`，Release 必须包含 Setup 安装包、对应 `.blockmap` 和 `latest.yml`。
- 版本升级到 `V6.2.0 Release`，重新生成 Windows Portable / Setup 安装包。

## V6.1.0 Release · 2026-08-13

- 修复 AI 消息和用户消息渲染链路中的 XSS 漏洞，原始 HTML 会转义显示为可见文本而不是被删除；统一使用 DOM 清洗后写入页面，阻止 `<script>`、事件属性和 `javascript:` 链接等注入向量。
- 附件预览改为 DOM 属性赋值渲染，文件名与图片地址不再直接拼接进 HTML，进一步降低用户输入导致的注入风险。
- 画布模式的固定替换通知成为 XSS 清洗逻辑的唯一例外，仅允许完全符合 `span.canvas-replace-notice` 固定格式的通知恢复渲染；其他 HTML 仍按原安全规则处理。
- 思考过程支持不依赖 Markdown 库的 `**加粗文本**` 渲染，其他 Markdown 标记保持为纯文本。
- 外观设置新增绿色、浅蓝、琥珀、玫红和 Claude 风格暖白五种强调色，并统一作用于 `index.html` 中原有的绿色交互元素。
- 修复浅色/深色视觉主题按钮不显示当前选中状态的问题；字体大小新增特大和巨大两档。
- 清除语音记录移动到语音聊天设置底部，并补充 A/B/C 三组预制语音的用途说明。
- 桌面协作新增默认关闭的自动传图；每轮 VAD 结束后若没有手动截图则自动截取鼠标所在显示器，截图失败时提示并继续发送纯文字请求。
- 关于页名称更新为 `AI Chat UI NEXT`，当前版本更新内容从 `UPDATE.md` 读取，并可展开查看其余历史版本记录。
- 版本升级到 `V6.1.0 Release`，重新生成 Windows Portable / Setup 安装包。

## V6.0.0 Release · 2026-08-11

- 此 Release 与同日发布的 `V6.0.0 Canary 3` 内容完全一致，是 Canary 3 完成后的正式发布版本。
- 修复桌面协作横向字幕移动后右侧新文字仍被截断的问题，改为使用字幕容器的真实横向滚动并在流式文字更新时重新计算显示位置。
- AI 字幕仅在 TTS 开始朗读当前可视区域中无法完整显示的语句时滚动；前面已完整显示的语句不会提前触发移动。
- 用户语音识别字幕会随部分识别结果实时滚到最右侧，持续显示最新文字。

## V6.0.0 Canary 3 · 2026-08-11

- 修复桌面协作横向字幕移动后右侧新文字仍被截断的问题，改为使用字幕容器的真实横向滚动并在流式文字更新时重新计算显示位置。
- AI 字幕仅在 TTS 开始朗读当前可视区域中无法完整显示的语句时滚动；前面已完整显示的语句不会提前触发移动。
- 用户语音识别字幕会随部分识别结果实时滚到最右侧，持续显示最新文字。

## V6.0.0 Canary 2 · 2026-08-11

- 图片缩略图打开大图与关闭大图增加淡入淡出动画。
- 桌面协作模式在 AI 播放及回声冷却期间抑制 VAD，避免应用自我聆听 AI 声音；普通语音聊天逻辑不变。
- 偏好设置“桌面协作”新增“跳过B组语音”，默认关闭；开启后仅播放 A 组和 C 组预制语音。
- 桌面协作顶部 AI 行与用户行使用横向自动滚动，完整文本不再被省略号替代。

## V6.0.0 Canary · 2026-08-10

- 新增桌面协作模式：透明无边框、始终置顶、底部停靠的横向字幕窗口，并在主窗口与结束按钮之间实现选择性点击穿透。
- 桌面协作启动后自动最小化主窗口，结束后恢复；协作文本、AI 语音记录和截图会写回对应主会话。
- 新增可配置的全局传图快捷键，默认 `Ctrl+A`；截图目标为鼠标所在显示器，多次触发只保留最后一张。
- 新增低、均衡、高三档截图精细度，默认压缩为最大边 1920px、JPEG 质量 85。
- 桌面协作 system prompt 固定以前置隐藏提示说明截图能力，并将语音回复的首句及单句长度限制放宽为原来的两倍。
- 语音聊天和桌面协作统一支持 `/responses` 与 `/chat/completions`，Responses 返回 501 时自动回退重试。
- 语音预制包新增 C 组“让我看一下啊……”“稍等，我看看……”“欸？我看到了……”，截图请求在 A 组后必定随机播放一条 C 组语音。
- 新增常驻托盘菜单，可打开主页面、新建或恢复最近会话的语音聊天/桌面协作，并在开发者模式下清除或导出日志。
- 主窗口关闭后改为隐藏到托盘，只有托盘“退出AIUI”会结束应用。
- 版本升级到 `V6.0.0 Canary`，Windows 产物更新为 Canary 命名。

## V5.7.0 · 2026-08-07

- 修复对/chat/completions接口的回复内容渲染逻辑问题。
- 修复对/responses接口回复内容分类判定不精确的问题。
- 用户选择/responses端口若遇到501错误将自动回退/chat/completions。

## V5.6.0 2 · 2026-08-04

- 加入了KaTeX渲染的字体，显示更加精确。
- 增强密钥安全性。

## V5.6.0 · 2026-08-04

- 修复手动停止 AI 生成后，灵动岛通知仍然常驻的问题；现在会像正常回复结束一样在短暂时间后自动消失。
- 继续完善 Responses API 的流式输出兼容，已能正确返回非流式回复，并在收到增量内容时实时刷新消息内容。
- 版本升级到 `V5.6.0`，并重新生成 Windows Portable / Setup 安装包。

## V5.5.0 · 2026-08-03

- 修复启用 Clash 等 Windows 系统代理时，主进程 Fish Audio 国内版 TTS 未遵循 Electron 代理规则而持续鉴权失败的问题。
- 实时语音聊天 TTS 改为在 `audiochat.html` 渲染进程直接请求国内版接口，Network 面板可直接看到每句语音请求，并保留请求、返回状态和音频字节日志。
- 修复主对话回放并行读取多段 MP3 时长后误判总时长、只播放第一句的问题；现在逐段读取并按片段索引连续播放完整回复。
- 修复回放切换片段时清空旧音频触发旧 `onerror`、导致后续语音被重复推进或跳过的问题。
- 修复清除语音记录后播放灵动岛异常闪现，以及清除流程重复定义导致的重复反馈。
- 语音回放改为按原始句子顺序连续播放该次 AI 回复中全部成功生成的语音片段。
- 回放灵动岛压缩为单行通知高度，移除回复文字和播放状态，只保留半宽进度条、暂停 / 继续图标与停止图标。
- 修复语音聊天第一条语音尚未成功获取或播放时，AI 字幕错误定位到最后一句的问题；现在默认将第一句话置于中间。
- 版本升级到 `V5.5.0`，并重新生成 Windows Portable / Setup 安装包。

## V5.4.0 · 2026-08-03

- 语音聊天模式生成的 AI 语音会按条保存到 IndexedDB。
- 主对话里，来自语音聊天模式的 AI 回复现在会显示“回放”按钮。
- 新增顶部“灵动岛”式语音回放条，支持暂停、停止和拖动进度；切换到另一条回放时会自动停止当前播放。
- 偏好设置“其他”新增“清除语音记录”，会通过带淡入淡出动画的警告弹窗删除所有非预制语音记录。
- 版本升级到 `V5.4.0`，并重新生成 Windows Portable / Setup 安装包。

## V5.3.0 · 2026-08-02

- 语音聊天字幕字号和行距略微收紧。
- AI 字幕支持按句自动跟随滚动，并允许用户手动滚动；5 秒无操作后恢复自动跟随。
- AI 字幕改为“每句完成后淡入”，人类字幕取消淡入。
- AI / 人类字幕区域隐藏系统滚动条但保留滚动能力。
- 压缩上下文确认弹窗按钮改为纵向独占一行布局。
- 偏好设置和压缩上下文确认弹窗新增淡入 / 淡出过渡。

## V5.2.0 · 2026-08-02

- 图像生成 / 编辑支持在提示词中写入 `16:9`、`4:3`、`1:1` 等宽高比。
- 自动把宽高比映射为图像请求尺寸。
- 图像重试会保留原始提示词并重新计算比例参数。

## V5.1.0 · 2026-08-02

- 重构语音聊天窗口布局，采用上下双分区黑白字幕界面。
- 语音聊天继续使用 Silero VAD、讯飞流式听写和 Fish Audio HTTP TTS。
- 语音聊天支持把完整的人类 / AI 轮次回传到当前文字对话。
- 新增模型列表编辑与上下文压缩能力。

## V5.0.0 · 2026-08-02

- 新增 Electron 语音聊天入口和独立 `audiochat.html` 页面。
- 新增 Fish Audio 预制语音生成与主进程文件存储。
- 新增讯飞流式语音听写和 Fish Audio HTTP TTS。
- 增加 Windows Portable / Setup 构建配置。
